<svg xmlns="http://www.w3.org/2000/svg" width="840" height="104" viewBox="0 0 840 104" role="img" aria-labelledby="title">
  <title id="title">Node, Express, MongoDB, MySQL, SQLite, Supabase, Firebase, Square</title>
  <defs>
    <style>
      /* Hallmark: technical composition with a visible human judgment boundary. */
      :root {
      --ink: #172b46;
      --muted: #495e78;
      --paper: #eef3fa;
      --panel: #e2eaf5;
      --line: #b1c0d2;
      --accent: #99512c;
      --navy: #111e30;
      --white: #edf2fa;
      --pale: #b3c2d5;
      --copper: #e6a775;
        --font-body: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
        --font-display: Georgia, 'Times New Roman', serif;
      }
      text { font-family: var(--font-body); fill: var(--ink); }
      .display { font-family: var(--font-display); }
      .muted { fill: var(--muted); }
      .accent { fill: var(--accent); }
      .paper { fill: var(--paper); }
      .panel { fill: var(--panel); }
      .navy { fill: var(--navy); }
      .white { fill: var(--white); }
      .pale { fill: var(--pale); }
      .copper { fill: var(--copper); }
      .rule { fill: none; stroke: var(--line); stroke-width: 1; }
      .signal { fill: none; stroke: var(--accent); stroke-width: 2; }
      .bright { fill: none; stroke: var(--copper); stroke-width: 2; }
    </style>
  </defs>
  <rect x="0" y="0" width="840" height="104" class="paper"/>
  <rect x="0" y="0" width="4" height="104" class="accent"/>
  <text x="24" y="44" font-size="27" font-weight="600">Node</text>
  <text x="24" y="80" font-size="24" font-weight="600">Express</text>
  <path d="M216 20V84M535 20V84" class="rule"/>
  <text x="244" y="43" font-size="19">MongoDB</text>
  <text x="244" y="79" font-size="19" class="muted">MySQL</text>
  <text x="372" y="79" font-size="19" class="muted">SQLite</text>
  <text x="564" y="43" font-size="19">Supabase</text>
  <text x="699" y="43" font-size="19">Firebase</text>
  <text x="564" y="79" font-size="19" class="muted">Square</text>
</svg>
